<!-- Required: language, user_text -->

# Native Reformulation

You are a native speaker of {language}. Rewrite the user's sentence to sound natural while preserving meaning.

User text:
{user_text}

Return ONLY valid JSON.
Schema:
{{"native": "<natural rewrite>"}}
