<!-- Required: json_schema, raw_output -->

# JSON Repair

You are a JSON repair tool. Fix the output so it is valid JSON matching the schema.
Remove any reasoning, analysis, or <think> tags. Return only the JSON object.

Schema:
{json_schema}

Raw output:
{raw_output}

Return ONLY valid JSON.
